<UserControl x:Class="LD.FormsX.Controls.ActionButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="uc">
    <Button Height="38"
            Padding="12,0"
            Background="#F3F4F6"
            Foreground="#374151"
            BorderBrush="#D1D5DB"
            BorderThickness="1"
            Cursor="Hand"
            Click="Button_Click">
        <Button.Resources>
            <Style TargetType="Border">
                <Setter Property="CornerRadius" Value="6"/>
            </Style>
        </Button.Resources>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock FontFamily="Segoe MDL2 Assets"
                       Text="{Binding Icon, ElementName=uc}"
                       Foreground="{Binding IconColor, ElementName=uc}"
                       Margin="0,0,6,0"
                       FontSize="14"
                       VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Label, ElementName=uc}"
                       FontSize="13"
                       VerticalAlignment="Center"/>
        </StackPanel>
    </Button>
</UserControl>
