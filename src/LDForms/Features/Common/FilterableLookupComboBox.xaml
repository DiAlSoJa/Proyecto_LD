<UserControl x:Class="LD.FormsX.Features.Common.FilterableLookupComboBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             MinHeight="28"
             Focusable="False">
    <Grid>
        <ComboBox x:Name="cmbLookup"
                  Margin="0"
                  Padding="2"
                  MinWidth="360"
                  MaxDropDownHeight="320"
                  VerticalContentAlignment="Center"
                  IsEditable="True"
                  IsTextSearchEnabled="False"
                  StaysOpenOnEdit="True"
                  IsSynchronizedWithCurrentItem="False"
                  ItemsSource="{Binding FilteredItems, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  SelectedItem="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  Loaded="cmbLookup_Loaded"
                  SelectionChanged="cmbLookup_SelectionChanged"
                  PreviewKeyDown="cmbLookup_PreviewKeyDown"
                  LostKeyboardFocus="cmbLookup_LostKeyboardFocus"/>
    </Grid>
</UserControl>
