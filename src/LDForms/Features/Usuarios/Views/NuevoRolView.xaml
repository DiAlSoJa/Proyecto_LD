<Window x:Class="LD.FormsX.Views.Usuarios.NuevoRolView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rol" Height="660" Width="560"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        ContentRendered="Window_ContentRendered">

    <Border Background="White" CornerRadius="12"
            BorderBrush="#E5E7EB" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.12"
                              BlurRadius="20" ShadowDepth="4"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="56"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0"
                    Background="#233167"
                    CornerRadius="12,12,0,0"
                    MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid Margin="20,0">
                    <TextBlock x:Name="txtHeaderTitle"
                               Text="Nuevo Rol"
                               Foreground="White"
                               FontSize="16" FontWeight="SemiBold"
                               VerticalAlignment="Center"/>
                    <Button HorizontalAlignment="Right"
                            Click="BtnClose_Click"
                            Background="Transparent" BorderThickness="0"
                            Foreground="White" Cursor="Hand"
                            Width="32" Height="32">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE711;"
                                   FontSize="14" VerticalAlignment="Center"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Cuerpo -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                          Padding="24,20,24,4">
                <StackPanel>
                    <TextBlock Text="Nombre del Rol" FontSize="13"
                               Foreground="#374151" FontWeight="Medium" Margin="0,0,0,6"/>
                    <TextBox x:Name="txtRoleName"
                             Height="38" VerticalContentAlignment="Center"
                             Padding="10,0" FontSize="13"
                             BorderBrush="#D1D5DB" BorderThickness="1"
                             Margin="0,0,0,20"/>

                    <TextBlock Text="Permisos" FontSize="13"
                               Foreground="#374151" FontWeight="Medium" Margin="0,0,0,8"/>

                    <Border BorderBrush="#E5E7EB" BorderThickness="1"
                            CornerRadius="8" Padding="4" MinHeight="360">
                        <TreeView x:Name="treePermisos"
                                  Background="Transparent"
                                  BorderThickness="0"
                                  FontSize="13"/>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="2"
                    BorderBrush="#E5E7EB" BorderThickness="0,1,0,0"
                    Padding="24,16" CornerRadius="0,0,12,12"
                    Background="White">
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right" >
                    <Button x:Name="btnCancel"
                            Content="Cancelar"
                            Height="38" Padding="20,0"
                            Click="BtnClose_Click"
                            Background="#F3F4F6" Foreground="#374151"
                            BorderBrush="#D1D5DB" BorderThickness="1" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style>
                        </Button.Resources>
                    </Button>
                    <Button x:Name="btnSave"
                            Content="Guardar"
                            Height="38" Padding="20,0"
                            Click="BtnSave_Click"
                            Background="#233167" Foreground="White"
                            BorderThickness="0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
