<UserControl x:Class="LD.FormsX.Views.CheckList.Tabs.ResumenBateriasTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="#F8FAFC">

    <UserControl.Resources>
        <Style x:Key="GridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#EEF1F5"/>
            <Setter Property="Foreground" Value="#16263D"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="#D9DEE7"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style x:Key="GridCellStyle" TargetType="DataGridCell">
            <Setter Property="BorderBrush" Value="#E4E8EE"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style x:Key="GridRowStyle" TargetType="DataGridRow">
            <Setter Property="BorderBrush" Value="#E4E8EE"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Background" Value="White"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F8FAFC">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Filtros -->
        <Border Grid.Row="0"
                Background="White"
                CornerRadius="12"
                Padding="16"
                BorderBrush="#E5E7EB"
                BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="55"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="110"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Desde:" VerticalAlignment="Center" FontSize="13" Foreground="#374151"/>
                <DatePicker x:Name="dpBateriaDesde" Grid.Column="1" Height="38" FontSize="13" SelectedDateFormat="Short" BorderBrush="#D1D5DB" Background="White"/>
                <TextBlock Grid.Column="3" Text="Hasta:" VerticalAlignment="Center" FontSize="13" Foreground="#374151"/>
                <DatePicker x:Name="dpBateriaHasta" Grid.Column="4" Height="38" FontSize="13" SelectedDateFormat="Short" BorderBrush="#D1D5DB" Background="White"/>

                <Button x:Name="btnBuscar"
                        Grid.Column="6"
                        Height="38"
                        Padding="12,0"
                        Click="BtnBuscar_Click"
                        Background="#F3F4F6"
                        Foreground="#374151"
                        BorderBrush="#D1D5DB"
                        BorderThickness="1"
                        Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE721;" Foreground="#2563EB" Margin="0,0,6,0" FontSize="14"/>
                        <TextBlock Text="Buscar"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- Contenido: lista + detalle -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.65*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="1.1*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="White"
                    CornerRadius="12"
                    BorderBrush="#E5E7EB"
                    BorderThickness="1"
                    Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="txtStatusBaterias"
                               Grid.Row="0"
                               Text="{Binding StatusText}"
                               FontSize="12"
                               Foreground="#6B7280"
                               Margin="4,0,0,8"/>

                    <Border Grid.Row="2" BorderBrush="#D9DEE7" BorderThickness="1" CornerRadius="4" Background="White">
                        <DataGrid x:Name="dgBaterias"
                                  AutoGenerateColumns="False"
                                  SelectionMode="Single"
                                  SelectionUnit="FullRow"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  IsReadOnly="True"
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="Vertical"
                                  RowHeight="28"
                                  ColumnHeaderHeight="40"
                                  FontSize="12"
                                  VerticalGridLinesBrush="#D9D9D9"
                                  HorizontalGridLinesBrush="#E4E8EE"
                                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                                  SelectionChanged="dgBaterias_SelectionChanged">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="No. Batería"   Binding="{Binding EquipmentName}"    Width="130"/>
                                <DataGridTextColumn Header="Serie"         Binding="{Binding SerialNumber}"     Width="120"/>
                                <DataGridTextColumn Header="Marca"         Binding="{Binding Brand}"            Width="110"/>
                                <DataGridTextColumn Header="Fecha y Hora" Binding="{Binding FechaDisplay}"     Width="130"/>
                                <DataGridTextColumn Header="Turno"         Binding="{Binding Turno}"            Width="90"/>
                                <DataGridTextColumn Header="Operador"      Binding="{Binding UserName}"         Width="150"/>
                                <DataGridTextColumn Header="Defectos"      Binding="{Binding TotalDefects}"     Width="85"/>
                                <DataGridTextColumn Header="Operativo"     Binding="{Binding OperativoDisplay}" Width="90"/>
                                <DataGridTextColumn Header="Proveedor"     Binding="{Binding SupplierName}"     Width="120"/>
                            </DataGrid.Columns>
                            <DataGrid.Resources>
                                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource GridHeaderStyle}"/>
                                <Style TargetType="DataGridCell"         BasedOn="{StaticResource GridCellStyle}"/>
                                <Style TargetType="DataGridRow"          BasedOn="{StaticResource GridRowStyle}"/>
                            </DataGrid.Resources>
                        </DataGrid>
                    </Border>
                </Grid>
            </Border>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="12"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="White"
                        CornerRadius="12"
                        BorderBrush="#E5E7EB"
                        BorderThickness="1"
                        Padding="10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Detalle" FontSize="12" Foreground="#6B7280" Margin="4,0,0,8"/>

                        <Border Grid.Row="2" BorderBrush="#D9DEE7" BorderThickness="1" CornerRadius="4" Background="White">
                            <DataGrid x:Name="dgBateriaDetalle"
                                      AutoGenerateColumns="False"
                                      SelectionMode="Single"
                                      SelectionUnit="FullRow"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      IsReadOnly="True"
                                      HeadersVisibility="Column"
                                      GridLinesVisibility="Vertical"
                                      RowHeight="28"
                                      ColumnHeaderHeight="40"
                                      FontSize="12"
                                      VerticalGridLinesBrush="#D9D9D9"
                                      HorizontalGridLinesBrush="#E4E8EE">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Tipo"  Binding="{Binding Tipo}"  Width="140"/>
                                    <DataGridTextColumn Header="Valor" Binding="{Binding Valor}" Width="220"/>
                                </DataGrid.Columns>
                                <DataGrid.Resources>
                                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource GridHeaderStyle}"/>
                                    <Style TargetType="DataGridCell"         BasedOn="{StaticResource GridCellStyle}"/>
                                    <Style TargetType="DataGridRow"          BasedOn="{StaticResource GridRowStyle}"/>
                                </DataGrid.Resources>
                            </DataGrid>
                        </Border>
                    </Grid>
                </Border>

                <Border Grid.Row="2"
                        Background="White"
                        CornerRadius="12"
                        BorderBrush="#E5E7EB"
                        BorderThickness="1"
                        Padding="12">
                    <StackPanel>
                        <TextBlock Text="Observaciones" FontSize="13" Foreground="#374151" Margin="0,0,0,6"/>
                        <TextBox x:Name="txtObservacionesBateria"
                                 Height="90"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"
                                 BorderBrush="#D1D5DB"
                                 IsReadOnly="True"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
